Flask==2.3.2               # For creating the web application
flask-cors==3.0.10         # Optional: Enable Cross-Origin Resource Sharing if needed
gunicorn==21.2.0           # For deploying on production servers (optional)
numpy==1.26.0              # If the ML model uses NumPy
pandas==2.1.3              # If the ML model uses Pandas for data processing
scikit-learn==1.4.2        # If the ML model is built with scikit-learn
tensorflow==2.13.0         # Example: If your ML model uses TensorFlow
transformers==4.34.0       # Example: If using Hugging Face models
torch==2.1.1               # Example: If using PyTorch models
requests==2.31.0  